<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <ListPreference
        android:defaultValue="@string/pref_classificacao_default"
        android:entries="@array/pref_classificacao_titulo"
        android:entryValues="@array/pref_unidade_valor"
        android:key="@string/pref_classificacao_key"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_classificacao_label" />
</PreferenceScreen>
